---
version: "3.8"
services:
  app:

    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

    command: make dev
    volumes:
      - "./app:/app/app"
      - "/app/node_modules"
      - ./app/Makefile:/app/Makefile

